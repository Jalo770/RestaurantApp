<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mad.restaurantapp">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".UI.LogIn"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".UI.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity
            android:name=".UI.MenuContent"
            android:label="@string/title_activity_menu_content"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UI.MenuAddItem"
            android:label="@string/title_activity_menu_add_item"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UI.OrderCreateItem"
            android:label="@string/title_activity_order_create_item"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UI.OrderContent"
            android:label="@string/title_activity_order_content"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UI.BookingEditItem"
            android:label="@string/title_activity_booking_edit_item"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>